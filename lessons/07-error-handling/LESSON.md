# Lesson 07 — Errors and paper gates

> Spine: IB error callbacks · Python exceptions

## You write

Risk gates that **reject** (size, live port, closed session stub).
Map a few IB `errorCode` values in `NOTES.md`.
Tests. No connect.

## Definition of done

A bad size is an error, not a log line you ignore.
