# Lesson 10 — CLI

> Spine: argparse

## You write

A `lob` CLI that reads JSONL depth, prints book + quote. Exit non-zero on live port flags.
Tests via subprocess or Click-style runner.

## Definition of done

You can replay a fixture from the command line.
